//find the perimeter of rectangle
#include<stdio.h>
int main()
{
	float length,area;
	printf(" Enter the  length of each side of  square(l):");
	scanf("%f",&length);
	area=length*length;
	printf("Area of square =%.3f",area);
	return 0;
	
}
